name: lotId
required: true
in: path
schema:
  type: string
  pattern: ^([0-9]{4})$
  example: '0001'
description: >-
  A multi-character numeric string containing the common number used to refer
  to the lot of a bbl.
