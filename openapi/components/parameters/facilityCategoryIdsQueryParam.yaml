name: facilityCategoryIds
required: false
in: query
schema:
  type: array
  items:
    type: integer
  minItems: 1
  example: [1, 2]
description: >-
  The ids of the FACDOMAINS (Domains) / Categories of the facilities
