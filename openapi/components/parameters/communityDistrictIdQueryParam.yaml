name: communityDistrictId
required: false
in: query
schema:
  type: string
  pattern: ^([0-9]{2})$
  example: '01'
description: >-
  The two character numeric string containing the community district id.
