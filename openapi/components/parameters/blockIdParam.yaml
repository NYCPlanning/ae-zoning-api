name: blockId
required: true
in: path
schema:
  type: string
  pattern: ^([0-9]{5})$
  example: '00047'
description: >-
  A multi-character numeric string containing the common number used to refer
  to the block of a bbl.
