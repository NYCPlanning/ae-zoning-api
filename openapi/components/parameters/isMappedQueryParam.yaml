name: isMapped
required: false
in: query
schema:
  type: boolean
  example: true
description: >-
  Used to filter whether a capital project or community
  board budget request has associated geographic
  coordinates.
