name: communityDistrictIds
required: false
in: query
schema:
  type: array
  items:
    type: string
    pattern: ^([0-9]{3})$
  minItems: 1
  maxItems: 10
  example: '101,204'
description: >-
  A list of three character numeric strings containing the concatenation of the borough and community district ids.
