name: boroughId
required: true
in: path
schema:
  type: string
  pattern: ^([0-9]{1})$
  example: '1'
description: >-
  A single character numeric string containing the common number used to refer
  to the borough.
