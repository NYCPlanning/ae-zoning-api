{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The concatenated borough and block ids of the tax lot.",
      "minLength": 6,
      "maxLength": 6,
      "example": "100047"
    },
    "type": { "type": "string", "enum": ["Feature"] },
    "geometry": {
      "type": "object",
      "description": "A geojson implementation of a MultiPolygon Simple Feature",
      "properties": {
        "type": { "type": "string", "enum": ["MultiPolygon"] },
        "coordinates": {
          "type": "array",
          "description": "Array of polygon coordinate arrays.",
          "items": {
            "type": "array",
            "items": {
              "type": "array",
              "items": {
                "type": "array",
                "description": "The fundamental spatial construct",
                "minItems": 2,
                "maxItems": 3,
                "items": { "type": "number" },
                "example": [-74.010835, 40.708442],
                "x-readme-ref-name": "Position"
              }
            }
          }
        }
      },
      "required": ["type", "coordinates"],
      "x-readme-ref-name": "MultiPolygon"
    },
    "properties": {
      "type": "object",
      "properties": {
        "boroughId": {
          "type": "string",
          "description": "The block code for the bbl",
          "pattern": "^([0-9]{1})$",
          "example": "1"
        },
        "blockId": {
          "type": "string",
          "description": "The block code for the bbl",
          "pattern": "^([0-9]{5})$",
          "example": "00047"
        }
      },
      "required": ["boroughId", "blockId"]
    }
  },
  "required": ["id", "type", "geometry", "properties"],
  "x-readme-ref-name": "TaxLotBlockGeoJson"
}
